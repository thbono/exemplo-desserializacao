# exemplo-desserializacao

Start Redis with Docker:

	docker-compose up -d

or

	docker stack deploy --compose-file docker-compose.yml redis


The application should run after the following command line:

	mvn test
